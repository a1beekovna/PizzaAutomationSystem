PizzaAutomationSystem

Автоматизированная система управления пиццерией на JavaFX + H2 + MVC + DAO

Приложение разрабатывалось как учебный проект для демонстрации архитектуры MVC, работы с базой данных, JavaFX, логирования и организации автоматизированного рабочего места оператора пиццерии.

Система позволяет управлять меню пицц, клиентами и заказами, а также сохраняет все данные в файл-базу H2 для постоянного хранения между запусками.

📌 Основные возможности

Управление меню (добавление, удаление, изменение пицц)

Управление клиентами (ФИО, телефон, история заказов)

Оформление заказов (пиццы, количество, итоговая стоимость)

Работа с H2 file-based database (данные сохраняются между запусками)

Отображение статистики заказов

Логирование всех ключевых операций (Log4j2)

Удобный JavaFX-интерфейс

Архитектура MVC + DAO

Полная автоматическая инициализация БД при первом запуске

🧱 Архитектура проекта
✔ MVC

Проект построен по модели MVC:

| Слой           | Файлы                                                 |
| -------------- | ----------------------------------------------------- |
| **Model**      | Pizza.java, Order.java, OrderItem.java, Customer.java |
| **DAO**        | PizzaDAODB.java, OrderDAODB.java, CustomerDAODB.java  |
| **View**       | FXML + CSS                                            |
| **Controller** | MainController.java                                   |

✔ Работа с базой данных

Используется H2 File-based база:

📌 Файлы автоматически создаются в папке:

/database/pizzeria.mv.db

Подключение выполняется в DatabaseConnection.java:

connection = DriverManager.getConnection(
"jdbc:h2:./database/pizzeria",
"sa",
""
);


⚙️ Технологии

| Технология           | Использование                          |
| -------------------- | -------------------------------------- |
| **JavaFX**           | Интерфейс, таблицы, формы              |
| **H2 Database**      | Локальное хранение данных              |
| **Maven**            | Управление зависимостями               |
| **DAO pattern**      | Работа с БД через отдельный слой       |
| **MVC architecture** | Разделение логики, интерфейса и данных |
| **Log4j2**           | Логирование ошибок и событий           |
| **Java 17**          | Основной язык разработки               |


📦 Установка и запуск
1️⃣ Установите JavaFX SDK

Скачайте JavaFX (17 или 21)

2️⃣ Убедитесь, что Maven установлен

